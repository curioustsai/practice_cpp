with section('format'):
  # How many spaces to tab for indent
  tab_size = 4

  # If a statement is wrapped to more than one line, than dangle the closing
  # # parenthesis on it's own line
  dangle_parens = True

  # Do not sort argument list
  autosort = False

with section('markup'):
  # no comment markup parsing and reflow
  enable_markup = False
